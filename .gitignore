# Firebase конфигурация за клиента (Firebase/RTDB + VAPID за push)
# Сложена е в репото, за да не 404-ва на Netlify.

# Локални / временни файлове
.DS_Store
Thumbs.db
*.log
.env
.env.local
node_modules/
